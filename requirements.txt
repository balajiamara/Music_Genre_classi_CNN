tensorflow==2.10.0
scikit-learn==1.3.0
numpy==1.24.3
matplotlib==3.7.2
seaborn==0.13.0
pandas==2.1.0
streamlit
librosa==0.10.1

# to runs the requirements.txt file command "pip freeze > requirements.txt"

#above moduels we have to install if "streamlit" and "librosa" not installed "py -m pip install streamlit librosa"

#to run the Music_Genre_App.py command "py -m streamlit run Music_Genre_App.py"

# upload the music file from Test_Music choose the file and click the "predict" button